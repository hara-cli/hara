{
  "name": "chrome",
  "version": "0.2.0",
  "description": "With explicit Chrome approval, let hara use the tabs and signed-in session of your running Chrome via chrome-devtools-mcp. Alternative to the isolated Playwright browser — enable one, not both.",
  "skills": ["skills"],
  "mcpServers": {
    "chrome": { "command": "npx", "args": ["-y", "chrome-devtools-mcp@latest", "--autoConnect"] }
  }
}
